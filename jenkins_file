pipeline {
  agent { node { label 'slave01' } }

   stages {
		steps {
          checkout scm
                       } 
           }
      
      stage('C') {
	  when { anyOf {
				environment name: 'LANGUAGE', value: 'C'
				environment name: 'LANGUAGE', value: 'All'
			}
	        }
		  }
	 }
